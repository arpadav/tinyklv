# TODO's

* `sentinel` to be list attribute
  * `val = <lit>`
  * `enc = <xcoder>`
  * no need for decoder, since the recognition sentinel is expected to be a literal.
  * currently, it was assumed that the encoder for the sentinel is the same as the key-encoder.

* `latebind`